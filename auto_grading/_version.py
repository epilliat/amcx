"""Version d'AMCx — source de vérité unique.

Lue par hatch (`[tool.hatch.version]` dans pyproject.toml) pour la version du
paquet, et par `amcx --version`. Ne pas dupliquer ailleurs.
"""

__version__ = "0.1.0"
